extern "C" __global__ void
replaceData(
    float *from, 
    float *to, 
    int offset, 
	int count)
{
	int index = blockDim.x * blockIdx.x + threadIdx.x;
	
	if (index < count)
	{
		to[index + offset] = from[index];
	}
}